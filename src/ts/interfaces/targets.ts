export enum NavTargets {
    GARAGE = 'garage-menu',
    WINNERS = 'winners-menu',
}

export enum GarageTotalTargets {
    TotalCount = 'garage-cars-count',
}

export enum GarageControlsTargets {
    GENERATE = 'generator-button',
    RACE = 'race-button',
    RESET = 'reset-button',
}

export enum GarageCreateTargets {}

export enum GarageUpdateTargets {
    COLOR = 'update-color',
    NAME = 'update-name',
    SUBMIT = 'update-submit',
}
export enum GarageTargets {
    START = 'start-engine-button',
    STOP = 'stop-engine-button',
    SELECT = 'select-button',
    REMOVE = 'remove-button',
}
